import React from 'react'

const practice = (props) => {
    return (
        <div>
            <h1>It's been {props.age} years and its still the same {props.name}</h1>
        </div>
    )
}

export default practice